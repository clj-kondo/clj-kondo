FROM ubuntu:latest

RUN apt-get update \
    && apt-get install -y curl

ARG TARGETARCH
ARG TARGETOS

COPY ${TARGETOS}/${TARGETARCH}/clj-kondo /bin/clj-kondo

RUN chmod +x /bin/clj-kondo

CMD ["clj-kondo"]
