(ns acme.lib.example)

(defmacro awful-macro [& _]
  ;; dark magic happening here
  )

(awful-macro [x y z a])

(defmacro awful-macro2 [& _]
  ;; dark magic happening here
  )

(awful-macro2 [x y z a])
